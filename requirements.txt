# Core
flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0

# Resume parsing
pdfplumber>=0.10.0
python-docx>=1.1.0

# Scraping & Search
requests>=2.31.0
pyyaml>=6.0.0
duckduckgo-search>=7.0.0
ddgs

# Browser automation (for PDF generation)
playwright>=1.44.0

# LLM (used only for prep phase)
groq>=0.5.0

# Markdown → HTML (for PDF generation)
markdown>=3.5.0

# Multi-Agent AI (CrewAI)
crewai>=0.120.0

# RAG — Vector store & Embeddings
chromadb>=0.6.0
sentence-transformers>=3.0.0

# Structured Output
pydantic>=2.0.0